# Optional image-generation override config.
# The shared defaults now live in `.env.local`.
# Copy to `.env.images.local` only if image generation needs different settings.

OPENROUTER_API_KEY="<server-only-secret>"
OPENROUTER_IMAGE_MODEL="openai/gpt-image-1"
